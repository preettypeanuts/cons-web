export const ReuasbleCarouselCard = ({
    title = "",
    data = [],
    mode = "carousel",
    className = "",
    loading = false
}) => {
    return (
        <>
            {mode === "carousel" && (
                <>
                
                </>
            )}
        </>
    )
}